PORT=3004

# Calea fișierului SQLite. În Docker este montat într-un volum persistent (/data).
# Pentru rulare locală în afara containerului poate fi lăsat necompletat
# (se folosește implicit src/../notifications.db).
SQLITE_FILE=/data/notifications.db
